{
  "version": 4,
  "system_prompt": "You are a concise, helpful coding assistant. Explore before you edit: prefer list_files and search_text over ls/grep/find for finding the right files, and read the relevant section of a file before changing it. Use tools when they help. When you change code, verify it before reporting done: run the project's test command with the bash tool and show the real result. Never claim it works on your word alone — if you haven't run it, run it.",
  "max_tool_steps": 20,
  "default_context_limit": 4000,
  "approval_tools": ["bash", "write_file", "edit_file"],
  "code_extensions": [
    ".py",
    ".js",
    ".ts",
    ".tsx",
    ".jsx",
    ".go",
    ".rs",
    ".java",
    ".rb",
    ".c",
    ".cpp",
    ".cc",
    ".h",
    ".hpp",
    ".cs",
    ".php",
    ".swift",
    ".kt",
    ".scala",
    ".sh"
  ],
  "verify_attempts": 3,
  "require_run": true,
  "max_item_chars": 4000,
  "file_injection": {"strategy": "head_tail", "budget": 4000, "tail_fraction": 0.5},
  "tool_output": {"strategy": "head_tail", "budget": 4000, "tail_fraction": 0.6},
  "compaction": {"strategy": "structured_checkpoint", "keep_head": 2, "keep_tail": 4, "trigger_fraction": 0.8, "summary_max_tokens": 1024},
  "retry": {"strategy": "backoff", "max_attempts": 3, "base_delay_ms": 100},
  "compaction_prompt": "You are a context summarizer. Compress the conversation below into a short checkpoint another model will use to continue. Preserve, verbatim, every concrete fact, code, name, decision, file path, and the current goal and next step. Drop chit-chat. Be terse but lose nothing the next turn needs.",
  "memory_search_limit": 5,
  "attach_pattern": "@(\\S+)",
  "temperature": 0.0,
  "max_tokens": 4096
}
